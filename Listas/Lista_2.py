def input_lista():
  """
  Função que solicita ao usuário que digite valores e os armazena em uma lista.

  Retorno:
    lista: A lista contendo os valores digitados pelo usuário.
  """
  lista = []
  numero_de_itens = int(input("Digite o número de itens na lista: "))

for i in range(numero_de_itens):
    valor = input(f"Digite o valor {i + 1}: ")
    lista.append(valor)

    max = max(list)
    print (max)